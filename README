danbooru-ruby-grabber is a danbooru downloader — simple script for download images from danbooru.donmai.us, konachan.com, e621.net, behoimi.org and yande.re. Support of any danbooru-powered site can be added easily.

Usage: danbooru.rb [options] "tags"
    -b, --board                      Where from to download. Supported options: danbooru (default), konachan, e621, behoimi, yandere
    -P, --pool POOLID                Pool ID (tags will be ignored)
    -w, --wget                       Use wget for download
    -c, --curl                       Use curl for download
    -s, --storage DIR                Storage mode (all images in one dir and symlinks in tagged dirs)
    -u, --user USERNAME              Username
    -p, --password PASSWORD          Password

Please always use -u and -p options with danbooru, because they block requests without login and password.

You can use up to 2 tags with basic account. Images are stored in tag-named directory. Also script creates file named files.bbs with all tags of each image.

You need json to be installed. You can install it by this command:

gem install json
Note: may be you need to use sudo.

Have fun.

Author: Xeron
E-mail: xeron.oskom@gmail.com
Homepage: http://xeron.13f.ru
